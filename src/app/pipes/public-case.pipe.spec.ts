import { PublicCasePipe } from './public-case.pipe';

describe('PublicCasePipe', () => {
  it('create an instance', () => {
    const pipe = new PublicCasePipe();
    expect(pipe).toBeTruthy();
  });
});
